<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.itrip.dao.ItripHotelOrderMapper">

    <sql id="Base_Column_List">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, userId, orderType, orderNo, tradeNo, hotelId, hotelName, roomId, count, bookingDays,
        checkInDate, checkOutDate, orderStatus, payAmount, payType, noticePhone, noticeEmail,
        isNeedInvoice, invoiceType, invoiceHead, linkUserName, bookType, creationDate, createdBy,
        modifyDate, modifiedBy
    </sql>



    <select id="queryAll" resultType="cn.itrip.pojo.ItripHotelOrder" parameterType="cn.itrip.dao.pojo.ItripHotel">
        select <include refid="Base_Column_List"/> from itrip_hotel_order
        where userId=#{userId}
        <if test="orderType!=null and orderType>=0">
            and orderType=#{orderType}
        </if>
        <if test="orderStatus!=null and orderStatus>=0">
            and orderStatus=#{orderStatus}
        </if>
        <if test="orderNo!=null and orderNo!=''">
            and orderNo=#{orderNo}
        </if>
        <if test="linkUserName!=null and linkUserName!=''">
            and linkUserName like concat("%",#{linkUserName},"%")
        </if>
        <if test="creationDate!=null and creationDate!=''">
            and creationDate=#{creationDate}
        </if>
        <if test="modifyDate!=null and modifyDate!=''">
            and modifyDate=#{modifyDate}
        </if>
        order by id desc
    </select>


    <select id="queryuserlinkuser" resultType="cn.itrip.pojo.ItripUserLinkUser" parameterType="cn.itrip.dao.pojo.ItripHotel">
      SELECT iulu.* FROM itrip_user iu,itrip_user_link_user iulu WHERE iu.id=iulu.userId
      <if test="linkUserName!=null and linkUserName!=''">
          and linkUserName like concat("%",#{linkUserName},"%")
      </if>
    </select>

    <insert id="adduserlinkuser" parameterType="cn.itrip.pojo.ItripUserLinkUser">
        INSERT INTO itrip_user_link_user VALUES(null,#{linkUserName},#{linkIdCard},#{linkPhone},#{token},#{creationDate},#{token},#{modifyDate},#{token},#{linkIdCardType});
    </insert>

    <delete id="deluserlinkuser" parameterType="java.lang.Long">
        delete from itrip_user_link_user where id in
        <foreach collection="array" open="(" close=")" separator="," item="ids">
            #{ids}
        </foreach>
    </delete>

    <update id="modifyuserlinkuser" parameterType="cn.itrip.pojo.ItripUserLinkUser">
        UPDATE itrip_user_link_user iu SET iu.linkUserName=#{linkUserName},iu.linkPhone=#{linkPhone},iu.linkIdCard=#{linkIdCard},iu.linkIdCardType=0 WHERE id=#{id};
    </update>
</mapper>